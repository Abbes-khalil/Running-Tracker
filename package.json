{
  "scripts": {
    "dev": "node scripts/dev.mjs",
    "dev:backend": "php -S 127.0.0.1:8000 -t frontend",
    "build": "vite build",
    "preview": "vite preview",
    "serve:production": "php -S 127.0.0.1:8080 -t dist"
  },
  "devDependencies": {
    "vite": "^8.1.3"
  }
}